# Project rules

## Writing style

- Never use em dashes (the `U+2014` character) anywhere: code, comments, strings, UI copy, docs, or commit messages. Use a hyphen with spaces (` - `), a comma, or a semicolon instead. This applies to every request, without being asked again.
